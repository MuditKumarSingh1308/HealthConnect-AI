# HealthConnect AI Backend

FastAPI application shell for HealthConnect AI.

No product features are implemented in Sprint 1.

## Setup

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```
